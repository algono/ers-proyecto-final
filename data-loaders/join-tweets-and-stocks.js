// TODO: Join the tweets and stocks data into a single JSON file that Astro can load. This script should read the JSON files generated by the tweets and stocks data loaders, combine them into a single JSON structure, and save it in the following location that Astro can access: `../src/content/tweets.json`).
// This is the format of the final JSON file that Astro will load:
/*
[{
  ceo: string;
  tweet: string;
  date: z.coerce.date(), // ISO date string, converted to Date object by Zod
  company?: string; // Optional: If not provided we just use the stock company name (e.g. "TWTR" for "Twitter")
  stockCompany: string;
  stockChange: number;
  history: number[];
}];
 */